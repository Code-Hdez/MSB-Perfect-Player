# MSB-Perfect-Player — runtime dependencies
#
# Core
numpy
opencv-python
dxcam                   # Windows screen capture @ 60 Hz (preferred)
mss                     # Fallback screen capture (if dxcam fails)

# ROI tool
pynput

# ML ball detector
ultralytics

# Auto-batting controller output
vgamepad                # Virtual Xbox 360 controller via ViGEmBus

# Piecewise-affine batter calibration
scipy

# PyTorch with CUDA (GPU inference — much faster):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# PyTorch CPU-only (if no NVIDIA GPU):
#   pip install torch torchvision

# Optional – ONNX-only inference (lighter, no ultralytics needed):
#   pip install onnxruntime-gpu

# Optional – TOML config on Python < 3.11:
# tomli